%h1 Pages
%ul.record
  - @pages.each do |page|
    %li{:id => "id#{page.id}"}
      = link_to page.label, edit_admin_page_path(page)
      = link_to 'Delete', admin_page_path(page), :confirm => 'Are you sure?', :method => :delete, :class => 'ir delete'
%br
= link_to 'New Page', new_admin_page_path
|
= link_to 'Settings', edit_admin_settings_path